{
  "name": "libhonzo-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docmd dev",
    "test": "npm run build",
    "build": "docmd build",
    "preview": "npx serve site"
  },
  "dependencies": {
    "@docmd/core": "^0.8.4"
  }
}
